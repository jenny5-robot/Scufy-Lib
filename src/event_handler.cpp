#include "../include/event_handler.h"

jenny5_event t_event_handler::query_for_event(jenny5_event &event, int available_info)
{
	if (available_info == TYPE | PARAM1 | PARAM2)
	{
		std::cout << "pas";
	}
}